```
# 広義単調増加 { |x| x > v }
# index = b_ary.bsearch_index { |x| x > v }
# 狭義単調増加 { |x| x >= v }
# index = b_ary.bsearch_index { |x| x >= v }
# 広義単調減少 { |x| x < v }
# index = b_ary.bsearch_index  { |x| x < v }
# 狭義単調減少 { |x| x <= v }
# index = b_ary.bsearch_index { |x| x <= v }
```

のところで好きなモードにして使おう